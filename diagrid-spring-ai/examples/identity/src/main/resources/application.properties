spring.application.name=identity

server.port=8080

# The service /downstream calls on the caller's behalf. Point DOWNSTREAM_URL at a second copy of
# this app (`--server.port=8081`) to watch the same identity arrive at its /whoami.
downstream.url=${DOWNSTREAM_URL:http://localhost:8081/whoami}
